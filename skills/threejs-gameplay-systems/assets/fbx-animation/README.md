# FBX animation helper

Copy `FbxActor.ts` into a game at `src/systems/FbxActor.ts` when a skinned FBX or animated GLB must play clips.

Read `../../references/fbx-animation.md` before wiring. This file is not part of the default Vite scaffold — games without skeletal clips should not import it.
